version: '3.4'

services:
    netchk:
        build:
            context: .
        container_name: netchk-alpine
        hostname: netchk-alpine
        restart: always
        tty: true
        network_mode: "bridge"
